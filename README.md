# BFMC DashBoard

The project contains all the provided code for the BFMC DashBoard

# Requirements
```code
pip install -r requirements.txt
```

# How to run
```code
python main.py --xavierip <XAVIERIP> --xavierport <XAVIERPORT>
```